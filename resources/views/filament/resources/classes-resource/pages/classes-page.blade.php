<x-filament::page>
    {{-- @php
        $url = app('url')->current();
        $segments = explode('/', $url);
        $secondToLastSegment = $segments[count($segments) - 2];
    @endphp

    {{ $secondToLastSegment }} --}}
</x-filament::page>
